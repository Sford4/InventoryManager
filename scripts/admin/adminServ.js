angular.module('inventoryManager')
    .factory('AdminProductsServ', ['$http', '$q', '$localStorage', function($http, $q, $localStorage) {
    	var service = {};

    	return service;
}]);
    	

angular.module('inventoryManager')
    .factory('AdminTransactionsServ', ['$http', '$q', '$localStorage', function($http, $q, $localStorage) {
    	var service = {};

    	return service;
}]);